{
  "host": "${process.env.APP_HOST || '127.0.0.1'}",
  "port": "${process.env.PORT || 8081}",
  "proxy": {
    "enabled": false
  },
  "autoReload": {
    "enabled": true
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
